#include <stdio.h>
/**
 * main -  prints all single digit numbers of base 10
 *
 * Return: 0
 */
int main(void)
{
	int x = 48;

	for (x = 48; x <= 57; x++)
		putchar(x);
	putchar('\n');
	return (0);
}

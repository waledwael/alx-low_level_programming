#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: number could be positive or negative or zero.
 * Return: integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
